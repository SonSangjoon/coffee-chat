# Example Author input-only fixture

This directory contains fictional, non-canonical public-domain test input. It
is used only to exercise the remote GitHub Template → Candidate → instance
flow. The prose is intentionally neutral and carries no personal identity.
